SOUNDCLOUD_CLIENT_ID=
SOUNDCLOUD_REDIRECT_URI=cloudscrobble://oauth
SOUNDCLOUD_TOKEN_BROKER_BASE_URL=https://broker.example
# Shared app key matching the broker's APP_API_KEY. Upstream SoundCloud and
# Last.fm credentials belong only in the broker environment, never in the app.
CS_APP_API_KEY=
